export interface GetMyIdeasResponse {
  ideaId: number;
  auth: string;
  writerImageUrl: string;
  writerName: string;
  thumbnailImageUrl: string;
  title: string;
  price: number;
  specialization: string;
  saveCount: number;
  viewCount: number;
}
